version: '3'
services:
    api:
        image: cls-backend
        build: .
        networks:
            - backend
        ports:
            - "3000:3000"

    web-cli:
        image: cls-backend
        networks:
            - backend
        command: sh

    networks:
        backend:
        driver: bridge